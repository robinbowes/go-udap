# Astro Starlight Preview Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up a side-by-side local-only preview of the docs site rendered with Astro Starlight at `docs/site-starlight/`, leaving the existing Fumadocs site at `docs/site/` and its CI workflow completely untouched.

**Architecture:** A new sibling Astro project. Content is copied (not symlinked) from `docs/site/content/docs/` into `docs/site-starlight/src/content/docs/`. The Fumadocs-specific components in `index.mdx` are swapped for Starlight equivalents (`Aside`, `CardGrid`, `LinkCard`); the five how-to pages that use the custom `<HowTo>` component are rewritten as plain markdown with structural headings. No Tailwind. No basePath. No CI changes.

**Tech Stack:** Astro 5+, `@astrojs/starlight`, `@astrojs/mdx`, [Pagefind][pagefind] (bundled by Starlight), pnpm, Node 22.

**Spec:** `docs/superpowers/specs/2026-06-12-starlight-preview-design.md`

**Branch in use:** `docs/starlight-preview-spec` (created during brainstorming). Continue on this branch; do not start a new one.

[pagefind]: https://pagefind.app

---

## Why a docs preview doesn't follow strict unit-test TDD

This project is a documentation site. There is no application logic to write tests against; "does it work" means "does it render correctly in a browser." The closest analogue to a failing test → passing test loop is:

1. Run `pnpm astro check` — a content-collection + TypeScript check that fails if frontmatter is malformed, imports are missing, or links reference nonexistent pages.
2. Run `pnpm build` — fails on broken MDX, missing components, or content schema violations.
3. Run `pnpm dev` and visually verify in a browser — the only way to catch styling, layout, and navigation issues.
4. Run `pnpm build && pnpm preview` — the only way to verify Pagefind search, because Pagefind indexes at build time, not in dev.

Each task below uses these tools as its verification step in place of a unit test. The "test fails first, then passes" rhythm is replaced with "build/check should succeed at the end of the task, and the visual state should match the task description."

---

## File structure

The plan creates everything under `docs/site-starlight/`. Nothing outside that directory is touched.

```
docs/site-starlight/
├── .gitignore              # node_modules, dist, .astro
├── package.json            # astro + @astrojs/starlight + @astrojs/mdx + sharp
├── pnpm-workspace.yaml     # allowBuilds opt-in for esbuild + sharp
├── astro.config.mjs        # Starlight integration + sidebar
├── tsconfig.json           # extends astro/tsconfigs/strict
├── pnpm-lock.yaml          # generated by pnpm install
├── public/
│   └── examples/
│       ├── dhcp.conf       # copy of docs/site/public/examples/dhcp.conf
│       ├── static-ip.conf  # copy of docs/site/public/examples/static-ip.conf
│       └── wifi-wpa2.conf  # copy of docs/site/public/examples/wifi-wpa2.conf
└── src/
    ├── content.config.ts   # declares the docs collection (Starlight schema)
    └── content/
        └── docs/
            ├── index.mdx              # NEW — Starlight-native, replaces placeholder
            ├── tutorials/             # bulk-copied from docs/site/
            ├── concepts/              # bulk-copied
            ├── reference/             # bulk-copied (incl. commands/ subdir)
            ├── contributing/          # bulk-copied
            └── how-to/
                ├── index.mdx                       # bulk-copied
                ├── capture-wire-traces.mdx         # bulk-copied
                ├── handle-permission-errors.mdx    # bulk-copied
                ├── install-go-udap.mdx             # bulk-copied
                ├── install-shell-completions.mdx   # bulk-copied
                ├── no-devices-found.mdx            # bulk-copied
                ├── onboard-over-wifi-ad-hoc.mdx    # bulk-copied
                ├── configure-for-dhcp.mdx          # REWRITTEN — was <HowTo>
                ├── configure-for-static-ip.mdx     # REWRITTEN — was <HowTo>
                ├── configure-wifi-wpa2.mdx         # REWRITTEN — was <HowTo>
                ├── discover-on-multi-nic-laptop.mdx# REWRITTEN — was <HowTo>
                └── recover-stuck-in-init-mode.mdx  # REWRITTEN — was <HowTo>
```

---

## Task 1: Scaffold the Astro + Starlight project

**Files:**
- Create: `docs/site-starlight/.gitignore`
- Create: `docs/site-starlight/package.json`
- Create: `docs/site-starlight/pnpm-workspace.yaml`
- Create: `docs/site-starlight/tsconfig.json`
- Create: `docs/site-starlight/astro.config.mjs`
- Create: `docs/site-starlight/src/content.config.ts`
- Create: `docs/site-starlight/src/content/docs/index.md` (temporary placeholder, replaced in Task 4)

- [ ] **Step 1: Create the directory**

```bash
mkdir -p docs/site-starlight/src/content/docs docs/site-starlight/public/examples
```

- [ ] **Step 2: Write the `.gitignore`**

File: `docs/site-starlight/.gitignore`

```
node_modules/
dist/
.astro/
pnpm-debug.log
```

- [ ] **Step 3: Write the `pnpm-workspace.yaml`**

This opts in to running build scripts for `esbuild` and `sharp`, which pnpm 11+ blocks by default. Astro depends on both.

File: `docs/site-starlight/pnpm-workspace.yaml`

```yaml
allowBuilds:
  esbuild: true
  sharp: true
```

- [ ] **Step 4: Write the `package.json`**

Use empty version strings so the engineer fills them via `pnpm add -E` in Step 6, which writes exact pins. Per the global CLAUDE.md `package.json` convention: pin exact, no carets.

File: `docs/site-starlight/package.json`

```json
{
  "name": "site-starlight",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check"
  }
}
```

- [ ] **Step 5: Write the `tsconfig.json`**

File: `docs/site-starlight/tsconfig.json`

```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"]
}
```

- [ ] **Step 6: Install Astro, Starlight, MDX, and Sharp**

```bash
cd docs/site-starlight
pnpm add -E astro @astrojs/starlight @astrojs/mdx sharp
```

Expected: pnpm installs the four packages and writes exact pins (no `^`) into `package.json`. `pnpm-lock.yaml` is created.

If pnpm warns about build scripts blocked for `esbuild` or `sharp`: the `pnpm-workspace.yaml` from Step 3 should have prevented this. Re-check the file is in the right place. The dev server will fail to start without them built.

- [ ] **Step 7: Write the Astro config with Starlight integration**

File: `docs/site-starlight/astro.config.mjs`

```js
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';
import mdx from '@astrojs/mdx';

export default defineConfig({
  integrations: [
    starlight({
      title: 'go-udap',
      social: [
        { icon: 'github', label: 'GitHub', href: 'https://github.com/yo61/go-udap' },
      ],
      sidebar: [
        // Populated in Task 2. For now the sidebar is empty.
      ],
    }),
    mdx(),
  ],
});
```

- [ ] **Step 8: Declare the content collection**

File: `docs/site-starlight/src/content.config.ts`

```ts
import { defineCollection } from 'astro:content';
import { docsLoader } from '@astrojs/starlight/loaders';
import { docsSchema } from '@astrojs/starlight/schema';

export const collections = {
  docs: defineCollection({ loader: docsLoader(), schema: docsSchema() }),
};
```

- [ ] **Step 9: Write a temporary placeholder homepage**

This proves the scaffold renders before any real content is copied. It is replaced in Task 4.

File: `docs/site-starlight/src/content/docs/index.md`

```markdown
---
title: Scaffold placeholder
description: Replaced in Task 4
---

If you can read this, the Starlight scaffold is working. Task 4 replaces this file.
```

- [ ] **Step 10: Verify `pnpm dev` starts and the placeholder renders**

```bash
cd docs/site-starlight
pnpm dev
```

Expected: server starts on `http://localhost:4321/`. Open it in a browser. You should see the default Starlight theme with the title "go-udap", a (currently empty) sidebar, and the placeholder text in the page body. No console errors.

Stop the dev server (Ctrl+C) before the next step.

- [ ] **Step 11: Verify `pnpm astro check` passes**

```bash
pnpm astro check
```

Expected: `0 errors, 0 warnings, 0 hints`.

- [ ] **Step 12: Commit**

```bash
git add docs/site-starlight/
git commit -m "docs: scaffold Astro Starlight preview project"
```

---

## Task 2: Configure the Diataxis sidebar

**Files:**
- Modify: `docs/site-starlight/astro.config.mjs`

- [ ] **Step 1: Replace the empty `sidebar:` array with the Diataxis ordering**

Edit `docs/site-starlight/astro.config.mjs`. Replace the empty `sidebar: []` line with:

```js
      sidebar: [
        { label: 'Tutorials',     items: [{ autogenerate: { directory: 'tutorials' } }] },
        { label: 'How-to guides', items: [{ autogenerate: { directory: 'how-to' } }] },
        { label: 'Concepts',      items: [{ autogenerate: { directory: 'concepts' } }] },
        { label: 'Reference',     items: [{ autogenerate: { directory: 'reference' } }] },
        { label: 'Contributing',  items: [{ autogenerate: { directory: 'contributing' } }] },
      ],
```

Note: Starlight v0.39+ requires the `items: [{ autogenerate: ... }]` wrapper form; the older bare-`autogenerate` form on a sidebar group is no longer accepted and produces a startup error. This plan was updated to reflect the current API after implementation surfaced the requirement.

The final file should look like:

```js
import { defineConfig } from 'astro/config';
import starlight from '@astrojs/starlight';
import mdx from '@astrojs/mdx';

export default defineConfig({
  integrations: [
    starlight({
      title: 'go-udap',
      social: [
        { icon: 'github', label: 'GitHub', href: 'https://github.com/yo61/go-udap' },
      ],
      sidebar: [
        { label: 'Tutorials',     items: [{ autogenerate: { directory: 'tutorials' } }] },
        { label: 'How-to guides', items: [{ autogenerate: { directory: 'how-to' } }] },
        { label: 'Concepts',      items: [{ autogenerate: { directory: 'concepts' } }] },
        { label: 'Reference',     items: [{ autogenerate: { directory: 'reference' } }] },
        { label: 'Contributing',  items: [{ autogenerate: { directory: 'contributing' } }] },
      ],
    }),
    mdx(),
  ],
});
```

- [ ] **Step 2: Verify `pnpm dev` shows the five sidebar groups**

```bash
pnpm dev
```

Expected: at `http://localhost:4321/`, the sidebar now shows the five group labels (Tutorials, How-to guides, Concepts, Reference, Contributing) in that order. Each group is currently empty because the content hasn't been copied yet — that's fine. The point is the configuration is correct. Stop the dev server.

- [ ] **Step 3: Commit**

```bash
git add docs/site-starlight/astro.config.mjs
git commit -m "docs: add Diataxis-ordered sidebar to Starlight config"
```

---

## Task 3: Copy `public/examples/` static assets

**Files:**
- Create: `docs/site-starlight/public/examples/dhcp.conf`
- Create: `docs/site-starlight/public/examples/static-ip.conf`
- Create: `docs/site-starlight/public/examples/wifi-wpa2.conf`

- [ ] **Step 1: Copy the three example config files**

```bash
cp docs/site/public/examples/dhcp.conf docs/site-starlight/public/examples/
cp docs/site/public/examples/static-ip.conf docs/site-starlight/public/examples/
cp docs/site/public/examples/wifi-wpa2.conf docs/site-starlight/public/examples/
```

- [ ] **Step 2: Verify the files are served by the dev server**

```bash
cd docs/site-starlight
pnpm dev
```

In another terminal:

```bash
curl -sf http://localhost:4321/examples/dhcp.conf | head -3
```

Expected: the first few lines of `dhcp.conf` print. (HTTP 200, file body returned.) Stop the dev server.

- [ ] **Step 3: Commit**

```bash
git add docs/site-starlight/public/examples/
git commit -m "docs: copy example .conf files into Starlight preview"
```

---

## Task 4: Write the Starlight-native `index.mdx`

This replaces the Task 1 placeholder. The new file has the same content as the existing Fumadocs `index.mdx` but uses Starlight components in place of Fumadocs components.

**Files:**
- Delete: `docs/site-starlight/src/content/docs/index.md`
- Create: `docs/site-starlight/src/content/docs/index.mdx`

- [ ] **Step 1: Delete the Task 1 placeholder**

```bash
trash docs/site-starlight/src/content/docs/index.md
```

(If `trash` is not installed, use `rm`. The file is the 5-line placeholder from Task 1.)

- [ ] **Step 2: Write the new `index.mdx`**

File: `docs/site-starlight/src/content/docs/index.mdx`

```mdx
---
title: go-udap
description: Discover and configure Squeezebox devices from the command line
---

import { Aside, CardGrid, LinkCard } from '@astrojs/starlight/components';

A small command-line tool that discovers and configures Squeezebox
devices over UDAP — the on-the-wire protocol Logitech's original
setup app spoke. One static binary, no Lyrion Music Server required
for setup, no Perl runtime, no GUI.

<Aside type="note">
  UDAP only talks to Squeezeboxes that are in *setup mode* (the light
  flashes red). Brand-new devices arrive in setup mode; existing
  devices can be put back in by holding the front button for 3–6
  seconds.
</Aside>

## Features

- **Discover** every Squeezebox on the LAN via UDP broadcast on port 17784
- **Read & write all 26 NVRAM parameters** — network, wireless, server pointer, hostname, and more
- **Query live network config** (`getip`) — actual IP / subnet / gateway, distinct from passive discovery
- **Multi-NIC aware** — pick one interface (`--bind-interface`) or fan out across all of them (`--all-interfaces`)
- **Shell completions** for bash, zsh, and fish, including MAC-address completion via short-timeout discovery
- **Single static binary**, ~2.8 MB, no runtime dependencies — macOS / Linux / Windows on amd64 and arm64
- **Round-trippable config** — `read | set` cycles cleanly through file or stdin

## Start here

<CardGrid>
  <LinkCard
    title="Tutorial — 5-minute setup"
    href="/tutorials/configure-your-first-squeezebox/"
    description="Walk a brand-new (or factory-reset) Squeezebox from setup mode to a working device pointed at your music server."
  />
  <LinkCard
    title="How-to guides"
    href="/how-to/"
    description="Task-oriented recipes: DHCP, static IP, Wi-Fi WPA2, multi-NIC discovery, recovery from init mode."
  />
  <LinkCard
    title="Reference"
    href="/reference/"
    description="Every subcommand, every flag, every NVRAM parameter, every exit code. Authoritative lookup."
  />
  <LinkCard
    title="Concepts"
    href="/concepts/"
    description="How UDAP discovery works on the wire, what the 26 NVRAM parameters mean, why discovery uses limited broadcast."
  />
</CardGrid>

## Install

```bash
brew install yo61/tap/go-udap
```

Or download a release binary from
[GitHub releases](https://github.com/yo61/go-udap/releases/latest) —
macOS, Linux, and Windows builds for amd64 and arm64.

## Protocol provenance

go-udap mirrors the Perl
[Net::UDAP](https://metacpan.org/dist/Net-UDAP) reference
implementation. Where the two diverge, Net::UDAP is the source of
truth. See [What is go-udap?](/concepts/what-is-go-udap/) for the full
lineage and the rationale for the rewrite.

## Source

[github.com/yo61/go-udap](https://github.com/yo61/go-udap)
```

Notes on what changed from the Fumadocs original:

- `import { Cards, Card } from 'fumadocs-ui/components/card'` and `import { Callout } from 'fumadocs-ui/components/callout'` — both replaced by `import { Aside, CardGrid, LinkCard } from '@astrojs/starlight/components'`.
- `<Callout type="info">` → `<Aside type="note">`.
- `<Cards>` → `<CardGrid>`.
- `<Card title=... href=... description=... />` → `<LinkCard title=... href=... description=... />`.
- All internal hrefs gained a trailing slash (Starlight's default routing uses trailing slashes; this avoids a redirect on click).

- [ ] **Step 3: Verify the page renders and the cards are clickable**

```bash
cd docs/site-starlight
pnpm dev
```

Open `http://localhost:4321/`. Expected:
- The Aside ("UDAP only talks to Squeezeboxes...") renders as a Starlight callout.
- The CardGrid shows four cards in a grid.
- Each card link is rendered (but clicking goes to a 404 for now — that's expected; Task 5 adds the destination pages).
- No console errors.

Stop the dev server.

- [ ] **Step 4: Verify `pnpm astro check` still passes**

```bash
pnpm astro check
```

Expected: `0 errors`. (Warnings about broken links to `/tutorials/...` etc. may appear; those resolve in Task 5.)

- [ ] **Step 5: Commit**

```bash
git add docs/site-starlight/src/content/docs/
git commit -m "docs: write Starlight-native index.mdx for preview"
```

---

## Task 5: Bulk-copy the non-rewritten content

Copy every MDX file under `docs/site/content/docs/` to `docs/site-starlight/src/content/docs/` except:

1. `index.mdx` — already written in Task 4.
2. The five how-to pages that use the `<HowTo>` custom component — rewritten in Task 6.

The other 35 files require no edits — frontmatter is identical between Fumadocs and Starlight, and they contain no Fumadocs-specific imports.

**Files:**
- Create: `docs/site-starlight/src/content/docs/tutorials/` (2 files)
- Create: `docs/site-starlight/src/content/docs/concepts/` (7 files)
- Create: `docs/site-starlight/src/content/docs/reference/` (5 top-level files + 9 in `commands/`)
- Create: `docs/site-starlight/src/content/docs/contributing/` (4 files)
- Create: `docs/site-starlight/src/content/docs/how-to/` (7 of 12 files copied here; the other 5 are rewritten in Task 6)

- [ ] **Step 1: Bulk-copy the whole-section directories**

```bash
cp -R docs/site/content/docs/tutorials    docs/site-starlight/src/content/docs/
cp -R docs/site/content/docs/concepts     docs/site-starlight/src/content/docs/
cp -R docs/site/content/docs/reference    docs/site-starlight/src/content/docs/
cp -R docs/site/content/docs/contributing docs/site-starlight/src/content/docs/
```

Expected: all four directories are copied recursively (including `reference/commands/`).

- [ ] **Step 2: Copy the seven simple how-to files**

```bash
mkdir -p docs/site-starlight/src/content/docs/how-to
for f in index capture-wire-traces handle-permission-errors install-go-udap install-shell-completions no-devices-found onboard-over-wifi-ad-hoc; do
  cp "docs/site/content/docs/how-to/${f}.mdx" "docs/site-starlight/src/content/docs/how-to/"
done
```

Note: there are twelve how-to files in total. The five that use `<HowTo>` (listed in the spec and in Task 6 below) are deliberately skipped here and rewritten in Task 6. The other seven — `index.mdx` plus six simple how-tos — are copied as-is.

- [ ] **Step 3: Verify the file count**

```bash
find docs/site-starlight/src/content/docs -name '*.mdx' | wc -l
```

Expected: `36`. (35 copied this task + 1 written in Task 4. The 5 missing how-tos arrive in Task 6 to total 41.)

- [ ] **Step 4: Verify `pnpm build` succeeds**

```bash
cd docs/site-starlight
pnpm build
```

Expected: `astro build` completes with no errors. Warnings about broken links to the five not-yet-copied how-to pages may appear — that's fine; Task 6 fixes them.

If a page fails to build because of an unexpected component import (something we missed in the brainstorming sweep), check `grep -lE 'fumadocs-ui|fumadocs-core' docs/site-starlight/src/content/docs/**/*.mdx`. If any matches show up, stop and report the unexpected import — the spec assumed only `index.mdx` and the five how-tos used Fumadocs components.

- [ ] **Step 5: Verify `pnpm dev` renders one page per section**

```bash
pnpm dev
```

Manually open each:

- `http://localhost:4321/tutorials/configure-your-first-squeezebox/`
- `http://localhost:4321/concepts/what-is-go-udap/`
- `http://localhost:4321/reference/exit-codes/`
- `http://localhost:4321/reference/commands/discover/`
- `http://localhost:4321/contributing/building-from-source/`
- `http://localhost:4321/how-to/install-go-udap/`

Each should render with the sidebar visible, the page content present, and no console errors. The sidebar group "How-to guides" should currently list seven pages (six copied here + the index page); Task 6 adds the remaining five. Stop the dev server.

- [ ] **Step 6: Commit**

```bash
git add docs/site-starlight/src/content/docs/
git commit -m "docs: copy non-rewritten content into Starlight preview"
```

---

## Task 6: Rewrite the five `<HowTo>` how-to pages as plain markdown

Each page is rewritten from the original `<HowTo>` JSX into headings (`## Goal`, `## Prerequisites`, `## Steps`, `## Verification`, `## Example config` where applicable). The "Notes" section that follows each `<HowTo>` is preserved verbatim — only the `<HowTo>` block itself is transformed.

The pattern is mechanical. The first rewrite below is shown in full; the other four follow the same template with values lifted from the source files.

**Files:**
- Create: `docs/site-starlight/src/content/docs/how-to/configure-for-static-ip.mdx`
- Create: `docs/site-starlight/src/content/docs/how-to/configure-for-dhcp.mdx`
- Create: `docs/site-starlight/src/content/docs/how-to/configure-wifi-wpa2.mdx`
- Create: `docs/site-starlight/src/content/docs/how-to/discover-on-multi-nic-laptop.mdx`
- Create: `docs/site-starlight/src/content/docs/how-to/recover-stuck-in-init-mode.mdx`

- [ ] **Step 1: Read the source for all five files**

```bash
cat docs/site/content/docs/how-to/configure-for-static-ip.mdx
cat docs/site/content/docs/how-to/configure-for-dhcp.mdx
cat docs/site/content/docs/how-to/configure-wifi-wpa2.mdx
cat docs/site/content/docs/how-to/discover-on-multi-nic-laptop.mdx
cat docs/site/content/docs/how-to/recover-stuck-in-init-mode.mdx
```

This is the source of truth for what goes into each rewrite. The rewrites preserve everything that was in the source (frontmatter, pre-`<HowTo>` prose, `<HowTo>` props, post-`<HowTo>` notes).

- [ ] **Step 2: Write the rewritten `configure-for-static-ip.mdx`**

File: `docs/site-starlight/src/content/docs/how-to/configure-for-static-ip.mdx`

```mdx
---
title: Configure a Squeezebox for static IP
description: Wired Ethernet with a fixed IP address
---

## Goal

Configure a Squeezebox to use a fixed IP address on wired Ethernet.

## Prerequisites

- Device connected by Ethernet to the same subnet as the dev machine
- Device in setup or init mode
- go-udap installed and on PATH
- Static IP details known: target IP, subnet mask, gateway, DNS
- Your Lyrion Music Server's IP address known

## Steps

1. Download the [example config](/examples/static-ip.conf) and edit the IP / subnet / gateway / DNS / LMS values to match your network.
2. Discover the device: `go-udap discover`
3. Apply: `go-udap set <mac> --config static-ip.conf --reboot`

## Verification

After ~15 seconds, run `go-udap getip <mac>`. The IP / Subnet / Gateway should match exactly what you put in the config.

## Example config

[static-ip.conf](/examples/static-ip.conf)

## Notes

- `lan_ip_mode=0` selects static IP. The four fields
  (`lan_network_address`, `lan_subnet_mask`, `lan_gateway`,
  `primary_dns`) become authoritative.
- Test the chosen IP isn't already in use on your network first
  (otherwise the device will appear to work but cause arp conflicts).
- If the device doesn't come back after reboot, the IP you chose
  isn't reachable. Factory-reset by holding the front button for 6+
  seconds and try again with a different IP.
```

Note: `exampleFile="/go-udap/examples/static-ip.conf"` in the original was a Fumadocs-specific download-link shortcut with the GitHub Pages basePath baked in. Here we drop the basePath (we're local-only) and surface it as a normal markdown link under "Example config".

- [ ] **Step 3: Write the rewritten `configure-for-dhcp.mdx`**

The source has a pre-`<HowTo>` prose paragraph and a fenced code block (`dhcp.conf` example). Both are preserved. Then the `<HowTo>` block is rewritten in the same shape as Step 2.

File: `docs/site-starlight/src/content/docs/how-to/configure-for-dhcp.mdx`

```mdx
---
title: Configure a Squeezebox for DHCP
description: Wired Ethernet, DHCP-assigned IP
---

A factory-fresh Squeezebox defaults to DHCP, so the minimal config
only needs to point it at your Lyrion Music Server.

## Example config

```ini title="dhcp.conf"
# Set your music server's IP:
server_address=192.168.1.250
```

## Goal

Configure a Squeezebox to use DHCP on wired Ethernet, pointed at your Lyrion Music Server.

## Prerequisites

- Device connected by Ethernet to the same subnet as the dev machine
- Device in setup or init mode (front-button hold 3-6s if needed; new devices are already in setup mode)
- go-udap installed and on PATH
- Your Lyrion Music Server's IP address known (e.g. 192.168.1.250)

## Steps

1. Save the example config above as `dhcp.conf` (or [download it](/examples/dhcp.conf)) and edit `server_address` to your LMS IP.
2. Discover the device: `go-udap discover`
3. Apply the config and reboot: `go-udap set <mac> --config dhcp.conf --reboot`

## Verification

After ~15 seconds, run `go-udap getip <mac>`. You should see a DHCP-assigned IP from your network's subnet.

## Notes

- Devices default to DHCP, so `lan_ip_mode=1` is omitted.
- On factory-fresh devices, `go-udap set` defaults to wired Ethernet
  when `--interface` is not passed (or wireless if `--wireless-ssid`
  is), so `interface=1` is omitted too. Requires go-udap v2.1.0+; on
  earlier versions, add `interface=1` to the config.
- To configure wireless or static IP instead, see [Configure a
  Squeezebox for Wi-Fi with WPA2](/how-to/configure-wifi-wpa2/) or
  [Configure a Squeezebox for static IP](/how-to/configure-for-static-ip/).
- The `--reboot` flag is necessary; network config changes don't take
  effect until the device reboots.
```

Note the duplicated "## Example config" + "## Goal" headings — that's intentional. The source had the prose+code-block example *above* the `<HowTo>` and the `exampleFile` prop *inside* it. Here we keep them in document order: prose example first, then the structured how-to flow. Internal links gained trailing slashes (Starlight convention).

- [ ] **Step 4: Write the rewritten `configure-wifi-wpa2.mdx`**

File: `docs/site-starlight/src/content/docs/how-to/configure-wifi-wpa2.mdx`

```mdx
---
title: Configure a Squeezebox for Wi-Fi with WPA2
description: Wireless networking with WPA2-PSK authentication
---

## Goal

Configure a Squeezebox to use Wi-Fi with WPA2-PSK.

## Prerequisites

- Device connected by Ethernet for the initial setup (Wi-Fi-first onboarding is tracked separately, see below)
- Device in setup or init mode
- go-udap installed and on PATH
- Your Wi-Fi SSID and pre-shared key known
- Your Lyrion Music Server's IP address known

## Steps

1. Download the [example config](/examples/wifi-wpa2.conf) and edit `wireless_SSID`, `wireless_wpa_psk`, and `server_address`.
2. Discover the device (still over Ethernet): `go-udap discover`
3. Apply: `go-udap set <mac> --config wifi-wpa2.conf --reboot`
4. Unplug the Ethernet cable.

## Verification

After ~30 seconds (Wi-Fi association takes longer than wired), re-run `go-udap discover`. The device should appear with its DHCP-assigned Wi-Fi IP. If it doesn't, plug the Ethernet back in to recover and check the SSID/PSK values.

## Example config

[wifi-wpa2.conf](/examples/wifi-wpa2.conf)

## Notes

- `interface=0` selects wireless. The example config sets this
  explicitly. On factory-fresh devices `go-udap set` will also infer
  `interface=0` from `wireless_SSID` if you omit it (and report the
  inference on stderr), but pinning it in the config removes any
  ambiguity.
- `wireless_wpa_mode=2` is WPA2; `wireless_wpa_mode=1` is WPA;
  `wireless_wpa_mode=0` is WPA-mixed-mode.
- WEP is also supported (`wireless_wep_on=1`, `wireless_wep_key=...`)
  but is insecure; don't use it on networks you care about.

## Wi-Fi-first onboarding (without Ethernet)

Initial onboarding of a factory-state device over Wi-Fi (using the
device's built-in ad-hoc network) is a separate procedure. See
[Onboard over Wi-Fi using ad-hoc mode](./onboard-over-wifi-ad-hoc.mdx).
```

- [ ] **Step 5: Write the rewritten `discover-on-multi-nic-laptop.mdx`**

The source has a pre-`<HowTo>` prose paragraph and post-`<HowTo>` notes; both preserved verbatim.

File: `docs/site-starlight/src/content/docs/how-to/discover-on-multi-nic-laptop.mdx`

```mdx
---
title: Discover on a multi-NIC laptop
description: Use --bind-interface or --all-interfaces when your dev machine has multiple network interfaces
---

If your laptop has Wi-Fi + wired Ethernet both up, default
`go-udap discover` only emits broadcasts on the kernel's
default-route NIC. If your Squeezebox is on the *other* interface's
subnet, you'll see zero devices.

## Goal

Make `go-udap discover` find a Squeezebox that's reachable only via a non-default NIC.

## Prerequisites

- go-udap installed and on PATH
- Multi-NIC dev machine (Wi-Fi + Ethernet, or multiple Ethernet)
- macOS or Linux — Windows multi-NIC discovery is not yet supported (tracked as Task #29)

## Steps

1. List your usable interfaces: `go-udap interfaces`
2. Either pick one explicitly: `go-udap --bind-interface en7 discover`
3. Or fan out across all of them: `go-udap --all-interfaces discover`

## Verification

You should now see the device's MAC in the output. Run `tcpdump -i any 'udp port 17784'` in another terminal to confirm the broadcast went out the NIC you expected.

## Which one should I use?

- **`--bind-interface NAME`** — when you know exactly which NIC reaches
  the device. Faster (one socket), simpler.
- **`--all-interfaces`** — when you're unsure. Sends concurrently on
  every interface; the device replies via whichever one reaches it.

The two flags are mutually exclusive. Combining them is a usage error
(exit code 1).

## What's actually happening on the wire?

See [Multi-NIC discovery](/concepts/multi-nic-discovery/) for the
underlying mechanics (`IP_BOUND_IF`, `SO_BINDTODEVICE`,
`SO_REUSEPORT`).
```

- [ ] **Step 6: Write the rewritten `recover-stuck-in-init-mode.mdx`**

This page has no `exampleFile`, so the "Example config" section is omitted.

File: `docs/site-starlight/src/content/docs/how-to/recover-stuck-in-init-mode.mdx`

```mdx
---
title: Recover a Squeezebox stuck in init mode
description: When a device won't leave init/setup state
---

A Squeezebox reports `State: init` when it hasn't yet completed
initial configuration. If a previous `go-udap set` was interrupted or
applied bad values, the device can sit in `init` indefinitely.

## Goal

Get a Squeezebox out of init state and into connected state.

## Prerequisites

- Physical access to the device
- go-udap installed
- Device connected to power and on the same LAN as the dev machine

## Steps

1. Confirm the device is reachable: `go-udap discover`
2. If the device is in init state and you want to start over: factory-reset by holding the front button for 6+ seconds. The light flashes rapidly red, then the device reboots into a clean factory state.
3. Re-run the [tutorial](/tutorials/configure-your-first-squeezebox/) from a clean state.
4. If the device DOESN'T appear in `discover` after factory reset, see [Troubleshoot "no devices found"](/how-to/no-devices-found/).

## Verification

After the tutorial completes successfully, `go-udap info <mac>` should show `State: connected` and a real DHCP IP (not 0.0.0.0).

## Why does this happen?

`init` is the device's default state until something — typically the
server-association handshake — confirms the device has joined a music
server. If `server_address` was set to an unreachable IP, the device
stays in init forever waiting.

Factory reset is the safe recovery: it wipes NVRAM back to defaults
and starts the setup state machine over.
```

- [ ] **Step 7: Verify the file count is now 41**

```bash
find docs/site-starlight/src/content/docs -name '*.mdx' | wc -l
```

Expected: `41`. This matches the spec's success criterion.

- [ ] **Step 8: Verify `pnpm build` succeeds with no errors and no link warnings**

```bash
cd docs/site-starlight
pnpm build
```

Expected: build completes with `0 errors`. Any warnings about broken internal links should now be resolved (Task 5 left some pending because the five rewritten pages didn't exist yet).

If `astro check` is configured to warn on broken links, also run:

```bash
pnpm astro check
```

Expected: `0 errors`.

- [ ] **Step 9: Eyeball each of the five rewritten pages in dev**

```bash
pnpm dev
```

Open each:

- `http://localhost:4321/how-to/configure-for-static-ip/`
- `http://localhost:4321/how-to/configure-for-dhcp/`
- `http://localhost:4321/how-to/configure-wifi-wpa2/`
- `http://localhost:4321/how-to/discover-on-multi-nic-laptop/`
- `http://localhost:4321/how-to/recover-stuck-in-init-mode/`

Each should show: heading levels rendering correctly, no leftover `<HowTo>` tag (which would render as nothing or as an unknown-element warning in the console), example config link present where applicable. Stop the dev server.

- [ ] **Step 10: Commit**

```bash
git add docs/site-starlight/src/content/docs/how-to/
git commit -m "docs: rewrite HowTo pages as plain markdown for Starlight preview"
```

---

## Task 7: Two-stage verification and live-site sanity check

This is the final pass. It produces no commits (unless it surfaces something to fix); its purpose is to confirm the spec's success criteria are met before the preview is handed back to the maintainer.

**Files:**
- None modified — this task is verification only.

- [ ] **Step 1: Verify `pnpm dev` renders cleanly**

```bash
cd docs/site-starlight
pnpm dev
```

Open `http://localhost:4321/`. Confirm:

- The sidebar shows the five Diataxis groups in order: Tutorials → How-to guides → Concepts → Reference → Contributing.
- The homepage renders with the Aside callout and the four LinkCards.
- One page per section opens cleanly (use the URLs from Task 5 Step 5 and Task 6 Step 9).
- The browser console shows no errors on any of the pages you opened.

Stop the dev server.

- [ ] **Step 2: Build for production and start the preview server**

```bash
pnpm build
pnpm preview
```

Expected: `pnpm build` completes with `0 errors` and reports a Pagefind index being generated (look for `[pagefind]` lines in the build output). `pnpm preview` serves the built `dist/` at `http://localhost:4321/`.

- [ ] **Step 3: Verify Pagefind search returns results**

Open `http://localhost:4321/` and press `Cmd+K` (macOS) or `Ctrl+K` (Linux/Windows). Type each of the following queries and confirm at least one result returns:

- `discover` — should match multiple pages (the command reference, the multi-NIC how-to, etc.).
- `static IP` — should match `configure-for-static-ip`.
- `broadcast` — should match the multi-NIC concept page and discovery-related pages.

If any query returns zero results: check that the Pagefind index was actually generated (look for `dist/pagefind/` directory and `pagefind-entry.json`). If the index is missing, re-run `pnpm build` and re-check the output for Pagefind errors.

Stop the preview server.

- [ ] **Step 4: Sanity-check that the live Fumadocs site still builds**

```bash
cd ../site
pnpm install --frozen-lockfile
pnpm build
```

Expected: the existing Fumadocs build succeeds with no errors. This proves nothing in `docs/site-starlight/` accidentally affected the live site.

- [ ] **Step 5: Confirm no files outside `docs/site-starlight/` were modified**

```bash
cd ../..
git diff --stat main..HEAD -- ':!docs/site-starlight/' ':!docs/superpowers/'
```

Expected: empty output. (The `docs/superpowers/` directory is excluded because the spec and plan files were committed there during brainstorming and writing-plans, both of which are out of scope for the implementation per se.)

- [ ] **Step 6: Report back**

The implementation is complete. Report to the maintainer:

- All 41 pages render (confirmed in dev and preview).
- Sidebar order is correct (visually confirmed).
- Pagefind search returns results for `discover`, `static IP`, and `broadcast` (confirmed in `pnpm preview`).
- Live Fumadocs site at `docs/site/` still builds.
- Branch `docs/starlight-preview-spec` contains all changes; push and PR when the maintainer is ready.

No further commit on this task unless a verification step surfaced something needing a fix.

---

## Self-review notes (for the engineer running this plan)

If anything in the plan turns out to be wrong (e.g., Starlight's `LinkCard` API has changed, Pagefind doesn't auto-bundle in the current Starlight version, the `astro/tsconfigs/strict` preset has been renamed), stop and report. Do not improvise the fix — re-check against the spec and the upstream docs, then update the plan before continuing.

Specifically:

- The plan assumes Starlight bundles Pagefind by default. As of Starlight 0.30+, this is true. If the maintainer's `pnpm add` lands on an earlier major, Pagefind may need explicit configuration; if you see no `[pagefind]` lines in the build output, check the installed Starlight version.
- The plan assumes `@astrojs/starlight/components` exports `Aside`, `CardGrid`, and `LinkCard` from a single entry point. This has been stable across recent versions but is worth a quick sanity check at the time of implementation if Step 3 of Task 4 fails with an unknown-export error.
- The plan assumes Starlight's content schema validates `title` and `description` frontmatter and accepts the existing files as-is. If any of the 35 bulk-copied files fail content-collection validation, the most likely cause is a frontmatter field that Fumadocs accepted but Starlight rejects. Inspect the failing file, compare its frontmatter to a known-good one, and adjust the failing file (do not change the Starlight schema).
